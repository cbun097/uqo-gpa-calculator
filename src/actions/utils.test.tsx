test('createArrayWithNumbers', () => {});

test('calculateGradePoints', () => {});

test('accumulatedCredits', () => {});

test('convertLetterToPoints', () => {});

test('semesterGradePoints', () => {});

test('finalPoints', () => {});

test('finalGPA', () => {});
